{
  "name": "wpr-gas-prices",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "WPR Wisconsin Gas Prices — newsletter digest image renderer + widget-logic tests (the widget itself is pure static HTML/JSON; this only bakes docs/digest.png for email).",
  "scripts": {
    "render:digest": "node scripts/render-digest.mjs",
    "test:widget": "node --test tests/widget-logic.test.mjs tests/data-contract.test.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
